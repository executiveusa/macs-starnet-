import fs from 'node:fs'; const needed=['config/city.json','config/policies.json','connectors/catalog.json','data/clients.json','data/content.json','data/tasks.json','data/crypto-watch.json']; for(const f of needed) JSON.parse(fs.readFileSync(f)); const skills=fs.readdirSync('skills/upstream/library').filter(x=>x.endsWith('.md')); if(skills.length<50) throw Error('skills import incomplete'); console.log(`check ok: ${skills.length} upstream library skills`);
