# Dispatch
MAXX is the single captain-facing dispatcher. Instinct is reached through a scoped relay. Outbound and callback credentials are separate; results are correlated, deduplicated, and recorded before status changes.
