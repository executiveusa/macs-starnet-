# Websites
Bluehost inventory reads and explicitly approved changes. Every change requires a before-state, idempotency key, after-state, and receipt. Production deployment remains a separate decision.
