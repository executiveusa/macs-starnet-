# Clients
Confirmed client records, service notes, owned domains, pending work, and receipts. The registry starts empty. Import only confirmed records and keep access tokens in the local secret store.
